<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
    <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" font-size="80" class="emoji">🧩</text>
    <style>
        svg { background-color: transparent; }
        .emoji { filter: none; }
        @media (prefers-color-scheme: dark) {
            svg { background-color: transparent; }
            .emoji { filter: grayscale(100%); }
        }
    </style>
</svg>